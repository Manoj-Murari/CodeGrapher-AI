import * as React from "react";

// This is a simplified placeholder to satisfy the import in App.tsx
const TooltipProvider = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export { TooltipProvider };